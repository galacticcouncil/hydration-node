// Placeholder for load_events() unit tests — wired up once the loop exists (W1/W2).
// use super::mock::MockClient;
// use crate::*;

// #[test]
// fn load_event_should_work() {
// 	let client = MockClient {};
// 	let lt = LiquidationTask::new(client, LiquidationTaskConfig::default());
//
// 	let events = lt.load_events(1);
// }
